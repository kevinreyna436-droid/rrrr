
import { Fabric } from './types';

// The Master Database is now empty to reduce processing overhead as requested.
export const MASTER_FABRIC_DB: Record<string, string[]> = {};

// INITIAL_FABRICS is empty by default.
export const INITIAL_FABRICS: Fabric[] = [];
